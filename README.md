# LSTM-Movie-Recommendation.
This project uses Long Short-Term Memory (LSTM) neural networks to predict the next movie a user might watch based on their viewing history. Unlike traditional recommendation systems that rely on collaborative filtering, this model treats movie-watching as a sequential pattern, learning user behavior over time.
